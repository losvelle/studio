import { FullSignalHistory } from '@/components/signal-history'; // Import the renamed component

export default function HistoryPage() {
  // The data fetching and state management are handled within FullSignalHistory
  return <FullSignalHistory />;
}
